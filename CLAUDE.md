# מודול הוראות רישום — פרויקט ערבה

מודול הוראות הרישום הוא חלק מפרויקט ערבה של משרד התחבורה, אגף הרכב ושירותי תחזוקה.
המודול מנהל את תהליך הנפקת הוראות רישום כלליות לכלי רכב מקצה לקצה — מרגע הגשת הבקשה על ידי היבואן ועד לרישום הראשוני במכון הרישוי.

## קבצים

- **RR.md** — תיאור תהליכי מקצה לקצה: 7 שלבים עיקריים, תהליכים תומכים (חידוש שנתי, חסימות גיל, מכסות), וממשקי מערכות (מסלול, רכבית, מכס).
- **RR.docx** — מסמך אפיון ודרישות מערכת: סמכות חוקית, ניהול משתמשים, מודל נתונים (33 שדות חובה), מנוע תהליכי עבודה, בקרת תקינה, ניהול מסמכים וממשקים.
- **RR.xlsx** — מבנה נתונים: שדות הוראת רישום, כללי ולידציה, משתמשים ותפקידים, שלבי התהליך, ומסמכים נדרשים.
